var options = {
  valueNames: [ 'name' ]
};

var userList = new List('products', options);

$(function () {
  $('[data-toggle="popover"]').popover({
      trigger: 'hover'
    
      
        });
    });
